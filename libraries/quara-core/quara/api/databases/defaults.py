"""Default variables for the quara database api."""

# By default no filters are used when querying.
DEFAULT_FILTER = None
# By default no projections are used when querying.
DEFAULT_PROJECTION = None
# By default only 1000 documents are returned.
DEFAULT_LIMIT = 1000
# By default no offset is set to query documents.
DEFAULT_OFFSET = 0
# By default no session is used
DEFAULT_SESSION = None
